/** @type {import('next').NextConfig} */
const nextConfig = {
  // App directory is now stable in Next.js 15
  reactStrictMode: false, // Disable strict mode to prevent hydration issues during development
}

module.exports = nextConfig